# Upload Status

WTS v2 is intended to include both WTS v1 Standard Wallet proof files and WTS v2 Direct/Open swap proof files.

The canonical file list is `FILE_MANIFEST.txt`.

The reference-source hash list is `REFERENCE_MANIFEST.sha256`.

All reference files in this package were copied from the verified `Compliance_Wallet_200.zip` source snapshot.
